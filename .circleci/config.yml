version: 2.1
executors:
  java:
    docker:
      - image: cimg/openjdk:11.0.13
    resource_class: medium

jobs:
  gradle-build:
    executor: java
    working_directory: ~/repo
    environment:
      JVM_OPTS: -Xmx3200m
      TERM: dumb
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "build.gradle" }}
            - v1-dependencies-

      - run: ./gradlew dependencies
      - save_cache:
          paths:
            - ~/.gradle
          key: v1-dependencies-{{ checksum "build.gradle" }}
      - persist_to_workspace:
          root: .
          paths:
            - .
      - store_artifacts:
          path: build/libs
  docker-build:
    executor: java
    steps:
      - attach_workspace:
          at: .
      - setup_remote_docker:
          version: 20.10.14
      - run:
          name: build images and push to registry
          command: |
            docker build -t app .
            docker login -u beenkim -p $DOKER_HUB_PASSWORD
            docker push beenkim/springbootcicdtest
            sudo apt-get update

workflows:
  main-branch-workflow:
    jobs:
      - gradle-build:
          context: org-global
          filters: &filters-ops
            branches:
              ignore: /.*/
            tags:
              only: /^v.*/
      - docker-build:
          context: org-global
          requires:
            - gradle-build
          filters:
            <<: *filters-ops
